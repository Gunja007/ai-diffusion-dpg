# reach_layer/config/domain.yaml — KKB domain config (local dev).
# In Docker/Kubernetes this file is replaced by dev-kit/configs/kkb/reach_layer.yaml.
# No KKB-specific overrides needed — dpg.yaml defaults apply.
