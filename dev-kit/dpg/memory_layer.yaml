# dev-kit/dpg/memory_layer.yaml
# DPG framework defaults for Memory Layer — same for any domain deployment.

server:
  host: "0.0.0.0"
  port: 8002

memory:
  session_ttl_seconds: 3600   # 1 hour
  max_sessions: 1000
