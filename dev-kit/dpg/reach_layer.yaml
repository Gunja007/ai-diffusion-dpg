# reach_layer/config/dpg.yaml — DPG framework defaults.

server:
  host: 0.0.0.0
  port: 8005

reach_layer:
  cli:
    prompt: "You: "
    agent_prefix: "Agent: "

agent_core_client:
  endpoint: "http://agent_core:8000/process_turn"
  timeout_s: 30.0
