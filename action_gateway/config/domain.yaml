# action_gateway/config/domain.yaml — KKB domain config (local dev).
# In Docker/Kubernetes this file is replaced by dev-kit/configs/kkb/action_gateway.yaml.

action_gateway:
  connectors:
    onest_market_lookup:
      endpoint: "http://localhost:9999/onest/market_lookup"
      timeout_ms: 5000
