apiVersion: v1
kind: Secret
metadata:
  name: {{ .Release.Name }}-secrets
  namespace: {{ .Release.Namespace }}
  labels:
    app: {{ .Release.Name }}
type: Opaque
# ANTHROPIC_API_KEY must be set before deploying.
# Create the secret manually:
#
#   kubectl create secret generic agent-core-secrets \
#     --from-literal=ANTHROPIC_API_KEY=<your_key> \
#     --namespace agent-core
#
# Or use an external secrets manager (AWS Secrets Manager, Vault, etc.).
# This file is a placeholder — do NOT store real keys here.
stringData: {}
